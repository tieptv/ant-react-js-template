server {
  listen 80;
  server_name localhost;

  location /vntrip/ {
    alias /usr/share/nginx/html/vntrip/;
    index index.html;
    try_files $uri $uri/ /vntrip/index.html;
  }

  location = /vntrip {
    return 301 /vntrip/;
  }
}
